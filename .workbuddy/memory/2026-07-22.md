# 2026-07-22

## MySQL 数据库初始化排错
- 用户尝试在项目中使用 MySQL，遇到一系列问题：
  1. `kkx` 用户没有 `meta`/`dw` 库的访问权限（只有 USAGE）
  2. SQL 脚本中 GRANT 给的是 `atguigu`，但项目实际用户是 `kkx`
  3. CMD 管道导入时 GRANT 语句因编码问题报 1410 错误
  4. `GRANT` 报错导致 SQL 脚本中断，后续 CREATE TABLE 未执行
- 修复措施：
  - 手动 GRANT kkx 对 meta 和 dw 的 ALL PRIVILEGES
  - 修改 meta.sql：CREATE DATABASE 加 IF NOT EXISTS，注释 GRANT 行，atguigu→kkx
  - 修改 dw.sql：注释 GRANT 行，atguigu→kkx
  - 两个脚本的 GRANT 注释掉后通过管道顺利导入

## 为 MySQL 概念答疑
- 解释了 Docker 与 MySQL 的关系、Engine 与 Session 的区别
- 解释了 Elasticsearch 在项目中的角色（全文关键词召回）
- 解释了为何向量存储和关键字检索不矛盾（各自索引不同数据）

## 生成并完善 MySQL 入门指南
- 创建 `MySQL入门指南.md`，面向零基础，后根据用户要求大幅扩充第一章基本原理
- 第一章（MySQL 基本原理）现包含 8 个小节：
  1.1 Excel 类比 → 1.2 关系型数据库核心理念（拆表 vs 大表）→ 1.3 三种表关系（一对多/多对多/一对一，均用项目实例）→ 1.4 主键外键 → 1.5 常用数据类型 → 1.6 SQL 四种分类（DDL/DML/DQL/DCL，标注在项目中的位置）→ 1.7 项目中的 MySQL 作用 → 1.8 元数据的意义（完整链路：用户问题 → meta 检索 → LLM 生成 SQL → dw 执行）
- 二三章保留基础使用和项目实操（Docker+PyCharm）
- 结合用户实际踩过的坑（GRANT/atguigu/管道编码）整理而成

## 排查 mysql_client_manager.py URL 错误
- `get_url()` 中 URL 前缀写成了 `http://`，应该是 `mysql+asyncmy://`
- SQLAlchemy 根据协议名加载驱动，`http` 不存在导致 `NoSuchModuleError`

## 解释连接池原理并注释 mysql_client_manager.py
- 解答了连接池是什么、为什么需要、内部如何实现（借-还队列 + pool_pre_ping）
- 为 mysql_client_manager.py 添加详细中文注释：
  - 文件级 docstring：画出了完整的数据访问链路（代码→SQLAlchemy→asyncmy→MySQL）
  - get_url()：逐个解释 URL 各组成部分的含义
  - init()：解释 create_async_engine 内部做的事（连接池 pool_size=5 / max_overflow=10）
  - 测试代码：4 步拆解，AsyncSession 的借用-归还机制逐行说明
  - 两个模块级实例的用途标注（meta vs dw）
