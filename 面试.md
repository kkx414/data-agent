# Python 同步 vs 异步 — 面试要点

---

## 一、核心区别

| | 同步（sync） | 异步（async） |
|---|---|---|
| 执行方式 | 一行跑完再跑下一行 | 遇到等待就切走，回头再继续 |
| 关键字 | 普通 `def` | `async def` + `await` |
| 适用场景 | CPU 计算、字符串拼接、对象创建 | 网络 I/O、数据库查询、文件读写 |
| 并发能力 | 单线程，排队执行 | 单线程，但等待时不阻塞，可切到其他任务 |

**一句话**：同步是"排队办事"，异步是"拿号后去旁边坐着等，叫到你再来"。

---

## 二、为什么需要异步？

网络 I/O 的时间远大于 CPU 计算：

```
同步耗时 = 请求1(100ms网络等待) + 请求2(100ms网络等待) + ... = 串行累加
异步耗时 = max(所有网络等待) ≈ 100ms（几乎并行）
```

Python 的异步用**单线程 + 事件循环**实现，避免多线程的锁和上下文切换开销。

---

## 三、async / await 的传染规则

```python
async def fetch():           # 异步：因为有 await
    resp = await http_call() # await = "我要在这里等，但你可以去干别的"
    return resp

async def caller():          # 异步：因为调用了异步函数
    data = await fetch()     # 传染上来了

asyncio.run(caller())        # 最外层：启动事件循环
```

**传染方向**：`await 底层I/O` → `async def 中间层` → `async def 上层` → `asyncio.run()`。单向，不可逆。

---

## 四、事件循环（Event Loop）— 核心机制

```
事件循环维护一个任务队列：

1. 取任务 A 执行
2. A 遇到 await → 暂停 A，注册"等 A 的 I/O 完成后唤醒"
3. 取任务 B 执行
4. B 遇到 await → 暂停 B，注册唤醒
5. A 的 I/O 完成 → 把 A 放回队列
6. 继续取任务...
```

**关键点**：所有切换发生在 `await` 处。不写 `await` 就不会切换，异步代码写得再复杂也是串行。

---

## 五、coroutine（协程）是什么

```python
async def foo():
    return 42

print(type(foo()))  # <class 'coroutine'>
```

- 调用 `async def` 函数**不会执行函数体**，而是返回一个 coroutine 对象
- coroutine 必须被 `await` 或 `asyncio.run()` 才会真正执行
- 面试高频题：**"下面的代码打印什么？"**

```python
async def get_data():
    print("fetching...")
    return "data"

result = get_data()      # 什么都不打印！只创建了一个 coroutine 对象
print(result)            # <coroutine object get_data at 0x...>
```

---

## 六、asyncio.run() 做了什么

```python
asyncio.run(main())
```

等价于：

1. 创建一个新的事件循环
2. 把 `main()` 协程放进去运行
3. 等待 `main()` 完成
4. 关闭事件循环

**每个线程同时只能有一个事件循环**。`asyncio.run()` 不能在已有事件循环的内部再调用。

---

## 七、项目中的实际对照

| 项目文件 | 同步方法 | 异步方法 | 原因 |
|---------|---------|---------|------|
| `mysql_client_manager.py` | `init()` | `close()` | init 只创建对象；close 要关闭 TCP 连接 |
| `build_meta_knowledge.py` | — | `build()` | 整条链路被 `session.execute()` 传染 |
| `dw_mysql_repository.py` | — | `get_column_type()` | `await session.execute(sql)` |
| `app_config.py` | 全部 | — | 纯 CPU：YAML 解析 + dataclass 创建 |

---

## 八、常见面试追问

**Q: 异步和多线程有什么区别？**

| | 异步 | 多线程 |
|---|---|---|
| 切换谁控制 | 代码显式 `await` 才切换 | 操作系统随时可切换（抢占） |
| 锁问题 | 无（单线程） | 需要 Lock 保护共享变量 |
| 适用 | I/O 密集型 | CPU 密集型 |
| 开销 | 极低（协程切换） | 较高（线程上下文切换） |

**Q: 为什么 Python 异步是单线程的？**

GIL（全局解释器锁）导致多线程在 CPU 密集型场景无法真正并行。异步在单线程内通过协作式调度绕开了 GIL，同时避免了多线程的竞态问题。

**Q: 什么时候不该用异步？**

- CPU 密集型计算（如图像处理、大循环）→ 用多进程
- 已有大量同步库，改造成本太高
- 简单的脚本，串行就够用

**Q: `await` 和 `yield` 的关系？**

两者都暂停函数执行并让出控制权。`yield` 返回一个值给调用者；`await` 等待一个可等待对象（coroutine / Task / Future）完成。Python 3.5+ 的 `async/await` 本质上是生成器的语法糖进化。

---

## 九、高并发 — 为什么异步是标配

### Q: 什么是高并发？

同一时间段内，系统需要**同时处理大量请求**的能力。

```
                              时间轴 →
  用户A ────[问]────────────[答] 
  用户B ────[问]────[答]
  用户C ────[问]──────────[答]
  用户D ────[问]──[答]
          ↑
      同一时刻 4 个请求同时到达
```

**并发 ≠ 并行**：

| | 并发（Concurrency） | 并行（Parallelism） |
|---|---|---|
| 定义 | 多个任务**交替**推进 | 多个任务**同时**执行 |
| 实现 | 单线程 + 时间片切换 | 多核 CPU 真正同时跑 |
| Python 异步 | ✅ 并发 | ❌ 不并行（单线程） |

### Q: 高并发下，同步模式为什么崩溃？

假设掌柜问数收到 1000 个用户同时问"华北销售额多少"，每个查询需等 MySQL 返回 50ms：

```
同步（线程池模式，假设开 10 个线程）：
  10 个线程同时处理 10 个请求 → 其余 990 个排队
  每个请求 50ms → 排队 5 轮 → 总耗时约 250ms
  
  线程数翻倍？→ 100 个线程 → 操作系统上下文切换开销剧增
  内存开销：每线程约 8MB 栈空间 → 1000 线程 = 8GB 内存（炸了）

异步（单线程 + 事件循环）：
  一个线程同时发起 1000 个 SQL 查询
  所有查询几乎同时发出去 → 同时等返回 → 同时收到 → 总耗时 ≈ 50ms
  内存开销：一个协程约 1KB → 1000 协程 = 1MB（几乎忽略不计）
```

### Q: 异步为什么有利于高并发？

**一句话：异步把等待时间"折叠"了。**

```
同步：用户A等50ms → 用户B等50ms → 用户C等50ms = 累加式浪费时间
异步：A发出查询 → 不等，立刻处理B → 不等，立刻处理C → ...
      50ms 后 A/B/C 的结果几乎同时回来
```

三个层面的优势：

| 层面 | 同步 | 异步 |
|------|------|------|
| **CPU 利用率** | CPU 空转等网络 | 等的时候切到其他请求 |
| **内存** | 每请求 ≈ 8MB（线程栈） | 每请求 ≈ 1KB（协程） |
| **创建开销** | 创建线程 ≈ 1ms | 创建协程 ≈ 微秒级 |

### Q: 本项目在哪些环节依赖异步实现高并发？

```
FastAPI（规划中）
  └── 每个 HTTP 请求自动分配一个协程
        └── LangGraph 工作流中 3 个 recall 节点并行 await
              ├── recall_column  → await Qdrant 查询（等 20ms）
              ├── recall_value   → await ES 查询   （等 15ms）
              └── recall_metric  → await Qdrant 查询（等 20ms）
        三个 I/O 操作并发执行，而不是串行 20+15+20=55ms
```

---

## 十、一行判断法则

```
会调用 await → async def → 调用者也要 async def → 一直传到 asyncio.run()
不会 await   → 普通 def  → 放哪里都行
```
